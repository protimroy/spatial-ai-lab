name = "spatial-ai-lab-proxy"
main = "index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# GEMINI_API_KEY is stored as a secret — never put it here.
# Set it with: wrangler secret put GEMINI_API_KEY
